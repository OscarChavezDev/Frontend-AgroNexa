export const environment = {
  production: true,
  // Relativo a propósito: el frontend llama al MISMO dominio desde el que se
  // abrió (con o sin www) → siempre es mismo-origen y el CORS deja de aplicar.
  apiUrl: '/api',
  analyticsId: 'G-5DTJ7S7E22'
};
